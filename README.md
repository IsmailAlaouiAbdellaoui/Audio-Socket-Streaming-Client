# Audio-Socket-Streaming-Client
This a WIP toy project written in C# to learn to create a media (audio) client-server architecture.

The server application (the other repository) is also needed to make it work.
